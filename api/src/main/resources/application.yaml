server:
  port: 8761

spring:
  application:
    name: Twisted Fate Eureka Server

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    renewal-percent-threshold: 0.85
